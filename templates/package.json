{
  "name": "alexa-skill-<%= name %>",
  "version": "0.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf ./dist && webpack -p",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "alexa-lambda-skill": "*",
    "babel-runtime": "^5.8.25"
  },
  "devDependencies": {
    "babel-loader": "^5.3.2",
    "webpack": "^1.12.2"
  }
}
