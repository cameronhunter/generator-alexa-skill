{
  "name": "alexa-skill-<%= fileName %>",
  "version": "0.0.0",
  "description": "An Alexa Skill for AWS Lambda",
  "main": "lib/index.js",
  "files": [
    "dist", "lib"
  ],
  "scripts": {
    "build": "rm -rf ./dist && webpack -p && bestzip dist/package.zip dist/index.js",
    "prepublish": "npm run build",
    "test": "mocha"
  },
  "dependencies": {
    "alexa-lambda-skill": "*"
  },
  "devDependencies": {
    "babel-loader": "^5.3.2",
    "bestzip": "^1.1.2",
    "chai": "^3.3.0",
    "mocha": "^2.3.3",
    "webpack": "^1.12.2"
  }
}
