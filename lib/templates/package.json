{
  "name": "alexa-skill-<%= fileName %>",
  "version": "0.0.0",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf ./dist && webpack -p",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "alexa-lambda-skill": "*"
  },
  "devDependencies": {
    "babel-loader": "^5.3.2",
    "webpack": "^1.12.2"
  }
}
